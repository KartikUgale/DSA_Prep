public class lengthOfArray {
    public static void main(String[] args) {
        String name = "my name is Kartik Ugale:)";
        System.out.println(name.length());
    }
}
// in String we need to type "variable.length()" to find total length of String.
// but in array we type "variable.length" to find total length of array.
// because in array ".length" is an property of array but in String ".length()"
// is functions for this reason we need to type ".length()" in this way.